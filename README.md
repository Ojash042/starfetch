# Starfetch
A basic nerdfetch inspired CLI program that uses ncurses to display the system information.

### Requirements
- Only runs on linux
- Requires nerdfont

### Usage
``` 
$ cmake -S . -B build/
$ cmake --build build/

```
